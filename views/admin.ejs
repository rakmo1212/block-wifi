<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
</head>
<body>
  <h1>Uploaded Images</h1>
  <div>
    <% images.forEach(image => { %>
      <div>
        <img src="<%= image.url %>" alt="Uploaded Image" style="max-width: 300px;" />
      </div>
    <% }) %>
  </div>
</body>
</html>
