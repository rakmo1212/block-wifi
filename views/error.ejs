<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f9f9f9;
    }

    .container {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      align-items: center;
      width: 100%;
      height: 100%;
      max-width: 1200px;
      padding: 20px;
      box-sizing: border-box;
    }

    .logo {
      display: flex;
      justify-content: space-between;
      align-items: center;
      width: 100%;
      max-width: 400px;
    }

    .logo img {
      height: 20px;
    }

    .circle {
      position: relative;
      width: 200px;
      height: 200px;
      border-radius: 50%;
      border: 6px solid #ffcccc;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .circle span {
      font-size: 20px;
      color: #ff3333;
      font-weight: bold;
      text-align: center;
    }

    .message {
      font-size: 18px;
      color: #333;
      text-align: center;
      margin-top: 20px;
    }

    .button {
      display: inline-block;
      margin-top: 20px;
      padding: 10px 20px;
      font-size: 16px;
      color: #fff;
      background-color: #3cb043;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      cursor: pointer;
    }

    .button:hover {
      background-color: #2e8b34;
    }

    .footer {
      font-size: 12px;
      color: #777;
      text-align: center;
    }

    @media screen and (max-width: 600px) {
      .circle {
        width: 150px;
        height: 150px;
      }

      .circle span {
        font-size: 16px;
      }

      .message {
        font-size: 16px;
      }

      .button {
        font-size: 14px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Logo Section -->
    <div class="logo">
      <img src="https://www.gstatic.com/images/branding/product/1x/google_fiber_512dp.png" alt="Google Fiber" />
      <span>Block WIFI</span>
    </div>

    <!-- Pulsing Error Section -->
    <div class="circle">
      <span>Oops!</span>
    </div>

    <!-- Error Message -->
    <div class="message">
      We are experiencing a rush of traffic right now. Please try again later.
    </div>

    <!-- Retry Button -->
    <a href="/" class="button">Try Again</a>

    <!-- Footer Section -->
    <div class="footer">
      All trademarks of Ookla, LLC, including Speedtest®, are used under license.
    </div>
  </div>
</body>
</html>
